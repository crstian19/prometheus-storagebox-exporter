# Hetzner API Token (required)
# Generate from: https://console.hetzner.cloud/ -> Security -> API Tokens
HETZNER_TOKEN=your-hetzner-api-token-here

# Optional: HTTP Server Configuration
LISTEN_PORT=9509
METRICS_PATH=/metrics
LOG_LEVEL=info

# Optional: Build Configuration (for docker-compose)
VERSION=latest
GIT_COMMIT=none
BUILD_DATE=unknown
